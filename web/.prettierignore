dist/
.astro/
.lighthouseci/
test-results/
playwright-report/
package-lock.json
public/data/
public/duckdb/
src/data/generated/

# Generated by tools/gen-codec-vectors.mjs and kept byte-identical to the [addon] lane's
# copy at addon/tests/fixtures/codec-vectors.json (fsb1.test.ts guards that). The bytes are
# `JSON.stringify(vectors, null, 2) + '\n'`; prettier reflows the short number arrays and
# would break the identity, so it does not own this file.
src/fixtures/addon/

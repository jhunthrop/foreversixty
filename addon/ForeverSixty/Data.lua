-- addon/ForeverSixty/Data.lua
-- Placeholder: the [data] lane's `pipeline addon-data` output (Task 4) replaces this file.
local _, ns = ...
ns = ns or {}

ns.Data = { build = "0", classes = {}, weights = {} }
return ns.Data
